# CLAUDE.md

このファイルは、本プロジェクト「Engine-Agent-AI」でAI（Claude）と作業する際のルールを定義する。
すべての作業はこのファイルの内容に従うこと。

## プロジェクト概要

UE（Unreal Engine）、Unity、Visual Studio / Visual Studio Codeなどの開発環境を直接起動し、
プロジェクトの解析・実装・ビルド検証・修正・改善をエージェントAIが行うデスクトップアプリケーション。

画面構成の3軸:
1. エージェントに指示するためのチャット欄（チャット内容の出力を含む）
2. 使用するエンジンの画面（UE / Unity のエディタウィンドウ）
3. ソースコード編集アプリの表示（Visual Studio / Visual Studio Code）

主な機能:
- 画面レイアウトの編集（各パネルのサイズ・配置変更。ウィンドウは独立させず、アプリ内に統合表示する）
- プロジェクトごとの起動（チャット履歴・使用エンジンをプロジェクト単位で切り替え）
- 会話履歴・作業履歴の永続保存（ユーザーが削除しない限り保持。保存可能なプロジェクトは最大20個まで）
- ユーザーアカウント機能（アカウントごとにデータを独立管理）
- ログイン画面 / 新規アカウント作成画面
- アカウント管理DB
- プロジェクト解析・改善提案・実行処理・ビルドテストを行うAIエージェント（Claude Agent SDK / Claude Code CLIを利用）

## 技術スタック（決定事項）

- **アプリ基盤**: Electron（デスクトップアプリとして構築）
- **フロントエンド**: React + TypeScript + Vite
- **バックエンド/メインプロセス**: Node.js + TypeScript（Electron Main Process内に実装）
- **データベース**: SQLite（ローカル永続化。アカウント管理・プロジェクトデータ・会話履歴を格納）
- **AIエージェント**: Claude Agent SDK / Claude Code CLI をNode.js側からサブプロセス/SDK経由で呼び出す

### エンジン/IDE画面統合の方針

ブラウザや通常のWeb技術では他アプリのネイティブウィンドウを直接埋め込むことはできないため、
以下の方式を採用する。

- UE / Unity / Visual Studio (Code) は**別プロセスのネイティブウィンドウ**として起動する
- Electron側のウィンドウマネージャモジュールが、OSのウィンドウ操作API経由でそれらのウィンドウ位置・サイズを
  アプリ内のレイアウト（パネル枠）に追従させ、常にアプリ内に「アタッチされているように見える」状態を維持する
- これは物理的なウィンドウ埋め込みではなく「位置・サイズの自動追従による疑似アタッチ」である。
  対応OS・対象アプリによって挙動差が出ることが分かった場合は、都度ユーザーに状況を報告し、対応方針を確認すること

## 絶対厳守ルール

1. **ユーザーとの会話は日本語で行うこと。**
2. **本プロジェクトはGitHubで管理する。**
   - 新規機能の追加・修正を行う際は、必ず作業用ブランチを分けて実装すること（`main`への直接コミットは禁止）
   - コミット・プッシュは自動で行ってよい
   - **PR作成後のマージは必ずユーザーの確認・承認を得ること。承認なしにマージしない**
3. **不確定要素を放置しない。** 仕様・設計・実装方針に曖昧な点がある場合は、必ずユーザーに確認を取り、
   状態の不透明さを解消してから作業を進めること
4. **ユーザーの作業が必要なときは、その手順・実行コマンド・確認方法を具体的に明記すること**
   （例: 環境構築、APIキー設定、外部サービスの認証など）
5. **エラーがある状態でコミット・プッシュは行わない。**
   コミット前に必ず lint / 型チェック / ビルド / テストを実行し、エラーがないことを確認すること。
   実行できない環境上の制約がある場合は、その旨を必ずユーザーに報告し、検証未実施であることを明示すること
6. **コードはオブジェクト指向を意識し、実装内容が一目で分かるコード・コミットログを心がけること**
   （クラス/責務分離を意識した設計、意味の通る命名、簡潔で目的が伝わるコミットメッセージ）

## 追加の運用ルール

### ブランチ運用

- 命名規則: `feature/xxx`（新機能）, `fix/xxx`（バグ修正）, `docs/xxx`（ドキュメント）, `chore/xxx`（雑務・設定変更）, `refactor/xxx`（リファクタリング）
- 1ブランチ1目的を基本とする。無関係な変更を混在させない

### コミットメッセージ規約（Conventional Commits準拠）

`<type>: <概要>` の形式を基本とする。type例: `feat`, `fix`, `docs`, `refactor`, `chore`, `style`, `test`

例: `feat: プロジェクト作成画面にバリデーションを追加`

### PR作成時の記載内容

- 変更概要（何を・なぜ変更したか）
- 動作確認方法（手動確認の手順、または実行したテスト）
- UI変更を伴う場合はスクリーンショット
- 既知の制約・未対応事項があれば明記

### コーディング規約

- TypeScriptは`strict`モードを基本とする
- 責務ごとにクラス/モジュールを分離する（画面レイヤー / DBアクセス層 / AIエージェント連携層 / ウィンドウ管理層を明確に分ける）
- コメントは「なぜそうしたか」が自明でない箇所のみに絞る。自明な処理内容の説明コメントは書かない
- 未使用のコード・デッドコードは残さない

### セキュリティ・データ管理

- APIキー・シークレット等は `.env` に格納し、**絶対にコミットしない**（`.env.example` にキー名のみ記載する）
- パスワードは必ずハッシュ化して保存する（平文保存禁止）。ハッシュ化アルゴリズムはbcryptまたはargon2を使用する
- アカウントごとのデータ（プロジェクト・チャット履歴・作業履歴）は、DB上で必ず `user_id` に紐付けて分離すること。
  他ユーザーのデータへ意図せずアクセスできる実装をしない
- 保存可能なプロジェクト数は1アカウントにつき最大20個。上限到達時は新規作成不可とし、
  既存プロジェクトの削除をユーザーに促すUI/エラーメッセージを用意すること
- 会話履歴・作業履歴はユーザーが明示的に削除しない限り永続的に保持する（自動削除・自動圧縮の実装は行わない）

## ディレクトリ構成（雛形）

```
Engine-Agent-AI/
├── CLAUDE.md                 # 本ファイル
├── README.md
├── package.json
├── electron/                 # Electron Main / Preload プロセス
│   ├── main.ts                # アプリ起動エントリポイント
│   ├── preload.ts             # Renderer⇔Mainのブリッジ（contextBridge）
│   ├── db/                    # SQLiteアクセス層
│   ├── auth/                  # 認証・アカウント管理
│   ├── agent/                 # Claude Agent SDK連携層
│   └── window-manager/        # エンジン/IDEウィンドウの追従配置管理
├── src/                      # Renderer プロセス（React）
│   ├── main.tsx
│   ├── App.tsx
│   ├── pages/
│   │   ├── Login/
│   │   └── Signup/
│   └── components/
│       ├── Layout/             # パネルレイアウト編集（サイズ・配置変更）
│       ├── ChatPanel/
│       ├── EnginePanel/
│       └── EditorPanel/
└── docs/
```

## ローカル開発環境セットアップ

**このリポジトリを操作するAI（Claude）自身の実行環境にはNode.jsが導入されていない。**
そのため `npm install` やビルド・型チェック・テストの実行は、Node.js環境が整うまで実施できない。
ユーザー側で以下のいずれかの方法でNode.js（LTS版を推奨）を導入すること。

```bash
brew install node
```

またはnvm経由:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
nvm install --lts
```

導入後、リポジトリルートで以下を実行して依存関係をインストールする:

```bash
npm install
```
