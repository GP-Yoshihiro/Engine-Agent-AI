/**
 * エージェントへの指示入力・チャット出力を行うパネル。
 * チャット履歴の表示・送信ロジックは次フェーズで実装する。
 */
function ChatPanel() {
  return (
    <div>
      <h2>チャット</h2>
      <p>エージェントとの対話はここに表示されます。</p>
    </div>
  );
}

export default ChatPanel;
